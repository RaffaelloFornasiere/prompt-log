import { ShineDirective } from './shine.directive';

describe('ShineDirective', () => {
  it('should create an instance', () => {
    const directive = new ShineDirective();
    expect(directive).toBeTruthy();
  });
});
